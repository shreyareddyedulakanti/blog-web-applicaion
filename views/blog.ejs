<%- include('partials/header.ejs') %>
  <% if (locals.post) { %>
<div class="container px-4 py-5 my-5 text-center shadow-sm">
    <h1 class="display-5 fw-bold text-body-emphasis"><%= post.title %></h1>
  </div>

  <div class="container-fluid container-xxl">
    <div class="row justify-content-center mb-5">
      <div class="col-md-10 p-4 bg-white min-vh-60 rounded">
        <div class="blog-content mb-4"><%=post.content %></div>
        <div><a href="/update/<%=post.id%>" class="btn btn-secondary">Edit</a>
        <a href="/delete/<%=post.id%>" class="btn btn-danger">Delete</a></div>
      </div>
      <% } %>
    </div>
  </div>

  <div class="container-fluid container-xxl">
    <div class="row justify-content-center align-items-center mb-5">
      <div class="col-auto"><a href="/" class="btn btn-primary">Go back to homepage</a></div>
    </div>
  </div>
<%- include('partials/footer.ejs') %>
